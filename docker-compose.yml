services:
  gym-tracker:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: gym-tracker
    ports:
      - "18080:3001"
    volumes:
      - gym-data:/data
    restart: unless-stopped

volumes:
  gym-data:
